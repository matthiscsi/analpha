interface:
  display_name: "analpha"
  short_description: "Fast yes/no verdicts for Codex workload checks"
  default_prompt: "Use $analphacodex as analpha for a compact approval or rejection verdict."

policy:
  allow_implicit_invocation: true
